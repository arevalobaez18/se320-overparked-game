using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurrencyContext : MonoBehaviour
{
    /* private currencyFormatterStrategy _strategy;

    public CurrencyContext(currencyFormatterStrategy strategy)
    {
        _strategy = strategy;
    }
    
    public string FormatAmount(float amount)
    {
        return _strategy.FormatCurrency(amount);
    }
    */
}
